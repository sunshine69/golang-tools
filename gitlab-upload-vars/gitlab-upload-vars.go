package main
/* Automate the uploading env vars to gitlab so we don't have to use web gui and too many click
Input would be an yaml file.

object:
	type: <group|project|instance>
	name: <object name>
	envars:
		KEY: VALUE

*/
import (
	"fmt"
)

func main() {

}
